zerops:
  - setup: api
    build:
      base: go@1
      prepareCommands:
        - npm install -g typescript
      buildCommands:
        - go build -o main server/main.go
      deploy:
        - main
    run:
      start: ./main

